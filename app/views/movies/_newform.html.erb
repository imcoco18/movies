<section class="movie-post">
  <h1>Post Movie</h1>

  <%= form_for(movie) do |f| %>
    <h4>映画タイトル </h4>
    <%= f.text_field :title %>
    <h4>感想</h4>
    <%= f.text_area :body %>
    <%= f.submit "Post" %>
    <% end %>
  </section>
